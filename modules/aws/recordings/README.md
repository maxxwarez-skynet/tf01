# recordings

Creates a private, encrypted, versioned S3 bucket and a bucket-scoped IAM access policy. Optionally attaches it to existing trusted AWS roles. No credentials or cross-cloud trust relationships are created.

See `variables.tf` for typed inputs and `outputs.tf` for the public interface. Use this module through the live roots and blueprints described in the repository README.
