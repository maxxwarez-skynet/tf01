# Deployment settings

Reads shared non-secret deployment JSON and derives resource names, tags, and the state-key prefix. Keep deployment metadata and backend keys aligned. The input is documented in `variables.tf`; outputs are in `main.tf`.
