# mysql

Creates MySQL Flexible Server with VNet integration, private DNS, TLS enforcement, and seven-day backups by default. No HA. Supply the administrator secret externally; it is stored in state.

See `variables.tf` for typed inputs and `outputs.tf` for the public interface. Use this module through the live roots and blueprints described in the repository README.
