# secrets

Creates an empty RBAC-enabled Key Vault with public access disabled, a private endpoint, private DNS, and purge protection. Secret creation and runtime read-role assignments are handled separately.

See `variables.tf` for typed inputs and `outputs.tf` for the public interface. Use this module through the live roots and blueprints described in the repository README.
